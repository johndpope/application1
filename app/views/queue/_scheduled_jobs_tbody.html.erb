<% if File.exists? (Rails.root.join('app', 'views', 'queue', 'kind', "_#{params[:name]}_scheduled_jobs_tbody.html.erb")) %>
  <%= render partial: "queue/kind/#{params[:name]}_scheduled_jobs_tbody", locals: { jobs: jobs } %>
<% else %>
  <%= render partial: "queue/common_scheduled_jobs_tbody", locals: { jobs: jobs } %>
<% end %>
