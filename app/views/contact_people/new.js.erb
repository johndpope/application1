<%
  modal = render partial: 'form', locals: {contact_person: @contact_person, resource_id: params[:resource_id], resource_type: params[:resource_type]}
%>

$("div.modal-backdrop.fade.in").remove();
$('#modal_contact_person').remove();
$('body').append('<%= escape_javascript(modal) %>');
$("#modal_contact_person").modal({show: true, backdrop: 'static', keyboard: false}).on('hidden.bs.modal', function (e) {
  $(this).remove()
});
$('.has-error').popover({  trigger: 'hover',  placement: 'bottom',  animation: true});
